const initialNotesReducer = {}

const notesReducer = (state=initialNotesReducer,action) => {

    switch(action.type){

        case(5): {

        }

        default:{
            return state
        }
    }
}

export default notesReducer